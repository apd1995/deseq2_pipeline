name: deseq2_pipeline
channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.11
  - r-base=4.5.2      # R 4.5.2 matches ds_env for performance
  - pip               # explicit pip dependency

  # Python packages — all from conda-forge
  - numpy>=1.24
  - pandas>=2.0
  - scipy>=1.10
  - anndata>=0.10
  - h5py>=3.8
  - psutil>=5.9
  - pytest>=7.4

# Note: R packages (DESeq2, glmGamPoi, data.table, R.utils) are installed
# directly inside R via pak (see setup.sh) since conda-forge does not yet
# have Bioconductor packages compatible with R 4.5.2
