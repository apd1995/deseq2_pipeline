name: deseq2_pipeline
channels:
  - conda-forge
  - bioconda
  - defaults

dependencies:
  - python=3.11
  - r-base=4.3
  - pip

  # Python packages — all from conda-forge
  - numpy>=1.24
  - pandas>=2.0
  - scipy>=1.10
  - anndata>=0.10
  - h5py>=3.8
  - psutil>=5.9
  - pytest>=7.4
